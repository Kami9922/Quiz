import styled from 'styled-components'

const QuestionsSumContainer = ({ className }) => {
	return (
		<div className={className}>
			<div>10</div>
		</div>
	)
}

export const QuestionsSum = styled(QuestionsSumContainer)``
