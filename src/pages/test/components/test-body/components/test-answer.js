import styled from 'styled-components'

const TestAnswerContainer = ({ className }) => {
	return (
		<div className={className}>
			<div>TestAnswer</div>
		</div>
	)
}

export const TestAnswer = styled(TestAnswerContainer)``
